[build]
  builder = "nixpacks"
  buildCommand = "npx next build --webpack"

[deploy]
  startCommand = "npx next start -p $PORT"
  healthcheckPath = "/"

[service]
  port = 3000

[mounts]
  data = "/app/data"
